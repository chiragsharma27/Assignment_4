<!DOCTYPE html>
<html lang="en">

<head>
    <%- include("./partials/header"); %>
        <title>
            <%= blog.title %> - Blog Details
        </title>
        <style>
            body {
                font-family: 'Poppins', sans-serif;
                background: linear-gradient(to bottom, #e3f2fd, #ffffff);
                color: #2c3e50;
                margin: 0;
                padding: 0;
            }

            .blog-header {
                background-color: skyblue;
                color: #ffffff;
                padding: 30px;
                border-radius: 10px;
                box-shadow: 0 6px 10px rgba(0, 0, 0, 0.15);
            }

            .blog-header h1 {
                font-size: 2.5rem;
                font-weight: bold;
            }

            .blog-header p {
                font-size: 1rem;
                color: #e1f5fe;
            }

            .blog-image {
                border-radius: 15px;
                box-shadow: 0 6px 10px rgba(0, 0, 0, 0.15);
                max-height: 400px;
                object-fit: cover;
            }

            .blog-content {
                background-color: skyblue;
                padding: 25px;
                border-radius: 15px;
                box-shadow: 0 6px 10px rgba(0, 0, 0, 0.15);
                white-space: pre-wrap;
                line-height: 1.7;
                font-size: 1rem;
                color: #455a64;
            }

            .btn-secondary {
                background-color: #0288d1;
                color: #ffffff;
                font-weight: bold;
                border: none;
                padding: 12px 20px;
                border-radius: 8px;
                transition: background-color 0.3s ease, transform 0.2s;
                font-size: 1rem;
            }

            .btn-secondary:hover {
                background-color: #0277bd;
                transform: scale(1.05);
            }

            nav a.navbar-brand {
                color: #ffffff !important;
            }

            nav a.nav-link {
                color: #b3e5fc !important;
            }

            nav a.nav-link:hover {
                color: #ffffff !important;
            }

            @media (max-width: 768px) {
                .blog-header {
                    text-align: center;
                }

                .blog-header h1 {
                    font-size: 2rem;
                }
            }
        </style>
</head>

<body>
    <%- include("./partials/navbar.ejs"); %>

        <div class="container mt-5" style="margin-top: 50px;">
            <div class="row justify-content-center">
                <div class="col-lg-8">
                    <div class="blog-header mb-4 text-center">
                        <h1 class="mb-3">
                            <%= blog.title %>
                        </h1>
                        <p class="text-muted mb-1">
                            <span>Category: <strong>
                                    <%= blog.category %>
                                </strong></span> |
                            <span>Date of Creation: <%= new Date(blog.createdAt).toLocaleDateString() %></span> |
                            <span>Author: <%= blog.createdBy.fullName || "Unknown" %></span>
                        </p>
                    </div>

                    <% if (blog.thumbnailImage) { %>
                        <img src="<%= blog.thumbnailImage %>" class="img-fluid blog-image mb-4" alt="<%= blog.title %>">
                        <% } %>

                            <div class="blog-content">
                                <%= blog.description %>
                            </div>

                            <div class="text-center mt-4">
                                <a href="/" class="btn btn-secondary mb-3">Back</a>
                            </div>
                </div>
            </div>
        </div>

        <%- include("./partials/scripts"); %>
</body>

</html>